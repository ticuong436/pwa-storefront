query productFilterFeatures($pillar: String) {
    productFilters(pillar: $pillar) {
        features {
            id
            title
            banner
            link
            link_status
        }
    }
}
